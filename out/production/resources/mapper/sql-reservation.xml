<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.hospital.baronic.mapper.ReservationMapper">
    <insert id = "insertReservationExcelData" parameterType="com.hospital.baronic.web.dto.ReservationSaveRequestDto">
        INSERT INTO baronic_db.reservation(
                                        patient_name
                                      , chart_Id
                                      , todo
                                      , dump
                                      , reservation_date
                                      , `position`
        )
        VALUES(
                #{patient_name}
              , #{chart_Id}
              , #{todo}
              , #{dump}
              , #{reservation_date}
              , #{position}
              )
    </insert>
</mapper>