<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.hospital.baronic.mapper.ReservationMapper">

    <insert id = "insertReservationExcelData" parameterType="com.hospital.baronic.web.dto.ReservationSaveRequestDto">
        INSERT INTO baronic_db.reservation(
                                        patient_name
                                      , chart_Id
                                      , todo
                                      , dump
                                      , reservation_date
                                      , `position`
        )
        VALUES(
                #{patient_name}
              , #{chart_Id}
              , #{todo}
              , #{dump}
              , #{reservation_date}
              , #{position}
              )
    </insert>

    <select id = "getReservationList" resultType="com.hospital.baronic.web.dto.ReservationResponseDto">
        SELECT reservation_id
            , patient_name
            , chart_id
            , todo
            , reservation_date
            , is_valid
            , is_modifying
            , dump
            , `position`
            , insert_time
            , update_time
        FROM baronic_db.reservation
    </select>

</mapper>