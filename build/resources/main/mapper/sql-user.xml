<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.hospital.baronic.domain.user.UserMapper">

    <select id = "isAlreadyID" parameterType="String" resultType="com.hospital.baronic.web.dto.UserDto">
        SELECT `id`
        , `passwd`
        , `salt`
        , `is_valid`
        , `phone_num`
        FROM baronic_db.admin_user
        WHERE 1=1
        AND id = #{id}
    </select>


    <select id = "isAlreadyPNum" parameterType="String" resultType="com.hospital.baronic.web.dto.UserDto">
        SELECT `id`
             , `passwd`
             , `salt`
             , `is_valid`
             , `phone_num`
        FROM baronic_db.admin_user
        WHERE 1=1
          AND phone_num = #{phone_num}
    </select>

    <select id = "get_SALT" parameterType="String" resultType="String">
        SELECT `salt`
        FROM baronic_db.admin_user
        WHERE 1=1
        AND id = #{id}
    </select>

    <insert id = "registerUser" parameterType="com.hospital.baronic.domain.user.User">
        INSERT INTO baronic_db.admin_user(id, passwd, salt, phone_num, is_valid)
        VALUES(
              #{id}
              , #{passwd}
              , #{salt}
              , #{phone_num}
              , 1
              )
    </insert>

    <update id = "updateSessionValue" parameterType="HashMap">
        UPDATE baronic_db.admin_user
        SET sessionId = #{sessionId}
        WHERE 1=1
        AND id = #{id}
    </update>

    <select id = "checkSessionId" parameterType="String" resultType="com.hospital.baronic.web.dto.UserDto">
        SELECT id
             , passwd
             , salt
             , phone_num
             , is_valid
             , sessionId
        FROM baronic_db.admin_user
        WHERE 1=1
        AND sessionId = #{sessionId}
    </select>

    <update id = "invalidSession" parameterType="String">
        UPDATE baronic_db.admin_user
        SET sessionId = NULL
        WHERE 1=1
        AND sessionId = #{sessionId}
    </update>

</mapper>